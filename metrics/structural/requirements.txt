# metrics/structural/requirements.txt
# Все зависимости уже в thesis-base (hdbscan, spacy, transformers, sentence-transformers).
diskcache    # кэш LLM-вызовов для M_contr (topic filter + contradiction check)
tqdm         # прогресс-бары по стадиям
