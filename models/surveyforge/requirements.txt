# models/surveyforge/requirements.txt
# SurveyForge-специфичные зависимости поверх thesis-base.
# Всё что уже есть в thesis-base (torch, transformers, sentence-transformers,
# openai, numpy, pandas, sklearn и т.д.) здесь не дублируется.

# ── Retrieval ──────────────────────────────────────────────────────────────────
faiss-cpu==1.9.0

# ── LangChain (требуется SurveyForge) ─────────────────────────────────────────
langchain==0.3.4
langchain-community==0.3.0
langchain-core==0.3.12
langchain-huggingface==0.1.0
langchain-openai==0.2.3
langchain-text-splitters==0.3.0

# ── Форматы данных ────────────────────────────────────────────────────────────
h5py

# ── Версионные пины (совместимость с gte-large-en-v1.5 custom new-impl) ────────
# gte-large-en-v1.5 использует кастомный new-impl с RoPE, который ломается
# на transformers > 4.44.x — rope_cos инициализируется с неправильным размером
transformers==4.44.2
tokenizers==0.19.1

# ── Прочее ────────────────────────────────────────────────────────────────────
jsonlines
tinydb==4.8.0
anthropic>=0.20
httpx>=0.27
aiohttp>=3.10
pydantic>=2.5
nltk>=3.9
